<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="10dp"
    android:background="@drawable/vehicle_item_background"
    android:layout_margin="5dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/vehicle_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_scooter" />

        <TextView
            android:id="@+id/vehicle_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:text="Xe Máy"
            android:textSize="16sp" />

        <ImageView
            android:id="@+id/check_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="end"
            android:src="@drawable/ic_check"
            android:visibility="gone" />
    </LinearLayout>

    <TextView
        android:id="@+id/vehicle_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:text="Vận chuyển mặt hàng nhỏ giá trị đến 3 triệu đồng"
        android:textSize="14sp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/vehicle_dimensions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:text="0.5 x 0.4 x 0.5 Mét - Lên đến 30 kg"
        android:textSize="14sp"
        android:visibility="gone" />
</LinearLayout>